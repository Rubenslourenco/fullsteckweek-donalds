import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

const HomePage = () => {
  return (
    <div>
      <Button>FWS</Button>
      <Input />
    </div>
  );
};

export default HomePage;
